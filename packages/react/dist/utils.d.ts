/**
 * Format a timestamp into a human-readable relative string.
 * Falls back to absolute locale string if too far in the past.
 */
export declare function formatTimestamp(ts: string | number | Date): string;
/**
 * Get initials for the avatar fallback (up to 2 chars).
 */
export declare function getInitials(name: string): string;
//# sourceMappingURL=utils.d.ts.map